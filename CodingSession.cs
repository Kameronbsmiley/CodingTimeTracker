/* TODO: Create this class so it will hold information about the different properties
*       - Id
*       - StartTime
*       - EndTime
*       - Duration
*/
class CodingSession
{
// Void for now, will decide what to return later since it's just going to be a "duration"
// Need to figure out if DateTime is the correct variable type for these
    public static void CalculateDuration(DateTime startTime, DateTime endTime)
    {
        // TODO: Take the two variables and return the difference (duration)
    }
}